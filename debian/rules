#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh $@

override_dh_auto_build:
	HOME=/tmp npm --cache /tmp/npmcache install
	dh_auto_build

override_dh_shlibdeps:

override_dh_auto_test:
   HOME=/tmp npm --cache /tmp/npmcache test
